syntax = "proto3";

package gbookshelf;

message Book {
    string title = 1;
    int32 page = 2;
    bool done = 3;
}

message Books {
    repeated Book books = 1;
}

message Void {}

service BookShelf {
    rpc List(Void) returns(Books) {}
    rpc Add(Book) returns(Book) {}
    rpc Remove(Book) returns(Void) {}
    rpc Update(Book) returns(Book) {}
}
